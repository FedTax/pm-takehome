/**
 * Client-safe demo constants (no secrets, no server-only imports).
 *
 * The store seeds a couple of example merchants with these fixed IDs so the
 * docs, the landing-page demo, and the dashboard always have something to show.
 */
export const DEMO_MERCHANT_ID = "mch_demo_coffee";
export const DEMO_MERCHANT_NAME = "Blue Bottle Coffee";
